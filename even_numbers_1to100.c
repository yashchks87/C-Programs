#include<stdio.h>
int main()
{
  int length;
  printf("Enter lenght of numbers\n");
  scanf("%d",&length);
  for(int i=1;i<=lenght;i++)
  {
    if(n%2==0)
    {
      printf("Even[%d]=%d",i,a[i]);
    }
  }
}
